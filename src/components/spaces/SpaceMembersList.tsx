/**
 * @deprecated This component has been moved to the spaces feature module.
 * Import from '@/features/spaces' instead.
 */

import { SpaceMembersList as FeatureComponent } from '@/features/spaces';

/**
 * Re-export the SpaceMembersList component from the spaces feature module.
 * This is provided for backward compatibility during the migration period.
 * 
 * @deprecated Use SpaceMembersList from '@/features/spaces' instead
 */
export const SpaceMembersList = FeatureComponent; 