/**
 * Shared Chat Components
 *
 * Platform-agnostic components used for both mobile and desktop chat views.
 * These components are part of the Phase 1 refactoring to support mobile/desktop split.
 */

export { MessageBubble, default as MessageBubbleDefault } from './MessageBubble';
export { MessageList, default as MessageListDefault } from './MessageList';
